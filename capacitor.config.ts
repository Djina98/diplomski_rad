import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elab.diplomski',
  appName: 'diplomski',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
